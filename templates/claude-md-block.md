<!-- ultra-dev:start — managed by /setup-ultra-dev. Re-run that skill to change it; manual edits inside these markers are overwritten. -->
<!-- ultra-dev:settings routing=__ROUTING_KEY__ tests=__TESTS_KEY__ docs=__DOCS_KEY__ adhd=__ADHD_KEY__ -->
## Workflow routing (ultra-dev)

__ROUTING__

Every feature lives in `docs/ultra-dev/<slug>/` (`spec.md`, `plan.md`, `notes.md`).
No skill auto-advances — each hand-off waits for an explicit `yes`.

### After a plan finishes

- **Tests** — __TESTS__
- **Docs** — __DOCS__
__ADHD__
<!-- ultra-dev:end -->
